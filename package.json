{
  "name": "zoream-server",
  "version": "1.0.0",
  "description": "Vercel serverless active user + game manager (Upstash KV optional)",
  "main": "api/index.js",
  "scripts": {
    "start": "vercel dev",
    "build": "echo build (serverless)",
    "electron": "electron ."
  },
  "dependencies": {
    "node-fetch": "^3.4.0",
    "cookie": "^0.5.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0",
    "electron": "^25.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
